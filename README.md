# RoMonito Stats Extension
RoMonitor Stats browser extension for https://roblox.com

## Installation Instructions
```npm install && npm run build```

To install the extension in Development Mode in Google Chrome; ensure Developer Mode is enabled in chrome://extensions, "Load Unpacked" on the same page and select the /dist folder to install.
